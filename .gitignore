# Vivado simulation artifacts (generated in tdc_gpx_ctrl.sim/)
xsim.dir/
*.log
*.pb
*.jou
*.wdb
*.vdb
*.rlx
.Xil/
dfx_runtime.txt
hs_err_*.dmp

# Adhoc xsim work directories created outside the xpr sim_1
/sim_face_seq_work/
/sim_mask_sweep/

/.agents/

# Claude Code local / machine state
/.claude/
/.claude/settings.local.json
/.claude/scheduled_tasks.lock

# Ad-hoc synthesis diagnostic script (per-session, not shared)
/scripts/run_synth_cs_opt.tcl
tmp/
/.tmp_*/

# Structured Vivado/xsim archives. Keep the policy README, ignore sessions.
/sim_results/vivado_xsim/sessions/

# Reproducible OOC synthesis/implementation sessions. Keep summaries in Git.
/signoff_results/sessions/
/signoff_results/matrices/
/sim_work/
